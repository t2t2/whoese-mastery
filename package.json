{
  "name": "whose-mastery",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:server",
    "dev:server": "nodemon --exec babel-node src/start.js"
  },
  "dependencies": {
    "babel-cli": "^6.7.7",
    "babel-preset-node5": "^11.0.2",
    "config": "^1.20.1",
    "feathers": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.9.1",
    "npm-run-all": "^1.8.0"
  }
}
